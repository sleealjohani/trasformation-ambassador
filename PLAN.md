# خطة المراحل ومعايير القبول

> تُحدَّث علامات الإنجاز بعد كل مهمة مقبولة. لا تبدأ مرحلة قبل موافقة صريحة على سابقتها.

---

## المرحلة صفر — التهيئة (لا ميزات) ✅ جاهزة للمراجعة

- [x] Next.js 16 + TypeScript صارم (`strict` · `noUncheckedIndexedAccess` · `exactOptionalPropertyTypes`)
- [x] Tailwind CSS 4 بنظام design tokens في `globals.css` ومصدر واحد في `src/lib/tokens.ts`
- [x] Drizzle ORM + drizzle-kit مهيّآن، و`db/schema.ts` بلا جداول بعد
- [x] Vitest للوحدات + Playwright للمسارات
- [x] لوحة الهوية الرسمية كاملة، والحالات الست بلون + رمز + نص
- [x] الطباعة: Janna LT بوزنين، تُحمَّل محليًا بلا أي شبكة خارجية
- [x] الشعار والنقش الرسميان في `public/brand` مع قواعد استخدامهما
- [x] المكوّنات السبعة في `src/components/ui` بكل حالاتها
- [x] صفحة `/kitchen-sink` تعرض المكوّنات والقيم
- [x] CI يشغّل lint + typecheck + test + build + budget + e2e
- [x] `.env.example` بلا أي سر حقيقي
- [ ] **ملفا خط Janna LT المرخّصان في `public/fonts`** — بانتظار الملفين من المؤسسة
- [ ] **ملف النسخة البيضاء الرسمية للشعار** — تُقلب النسخة الملوّنة برمجيًا مؤقتًا

**معايير القبول**

| المعيار | الحالة |
|---|---|
| الصفحة تعرض المكوّنات السبعة بكل حالاتها | ✅ `/kitchen-sink` |
| الفحوصات خضراء (lint · typecheck · test) | ✅ ٣١ اختبار وحدة |
| لا لون خارج اللوحة | ✅ يمنعه `tests/palette.test.ts` |
| أصغر مساحة لمس ٤٤×٤٤px | ✅ يتحقق منه اختبار Playwright |
| الحزمة الأولى < ١٨٠KB مضغوطة | ✅ ١٦٦KB |

---

## المرحلة ١ — النسخة الأساسية بلا ذكاء ⬜ لم تبدأ

- [ ] البوابات الأربع **بنموذج ثابت من ٣ حقول** (بلا ذكاء اصطناعي)
- [ ] `lib/redact.ts` — التنقية على الجهاز وعلى الخادم، بوحدة قابلة للاختبار لكل نمط
- [ ] رمز المتابعة: ٨ خانات بلا حروف ملتبسة (بدون O/0/I/1)، يُخزَّن مُلبّدًا فقط
- [ ] شاشة «متابعتي» بخط زمني للحالات وحذف بالرمز
- [ ] لوحة سفير التغيير: فرز · تصنيف · دمج · إحالة
- [ ] الأسئلة المتكررة وجسر المعرفة (قراءة فقط)
- [ ] مخطط قاعدة البيانات وترحيلاته
- [ ] حالات كل شاشة: فراغ · تحميل · خطأ · دون اتصال

**معايير القبول**

- اختبار Playwright يمر بمسار كامل: إرسال ← رمز ← متابعة ← تغيير الحالة من اللوحة ← ظهور الحالة للموظف.
- **لا عمود PII في المخطط** — يتحقق منه `tests/no-pii-columns.test.ts` في CI.
- اختبار يثبت أن النص الخام لا يصل إلى التخزين ولا إلى السجلات.

---

## المرحلة ٢ — الطبقة الذكية ⬜ لم تبدأ

- [ ] محادثة المتابعة: سؤال افتتاحي ثابت ثم ≤ ٣ أسئلة، سؤال واحد لكل رسالة و≤ ١٤ كلمة
- [ ] التصنيف والتجميع الدلالي (تشابه ≥ ٠٫٨٦ ⟶ دمج في قضية قائمة)
- [ ] «سمعت أن…» بأربع حالات فقط، ولا نشر قبل الاعتماد
- [ ] النبض الأسبوعي (رد واحد لكل جهاز أسبوعيًا)
- [ ] القضايا الحيّة والتصويت (مرة واحدة لكل جهاز لكل قضية)
- [ ] الحواجز: لا إفتاء · تنقية ومراجعة يدوية · خارج النطاق · تحوّل صامت عند تعطّل الخدمة

**معايير القبول**

- على عيّنة ١٠٠ مشاركة مراجعة يدويًا، دقة التصنيف مقبولة.
- كل ما دون ٠٫٦ ثقة يذهب إلى صندوق «يحتاج قراءة».
- **تعطيل خدمة الذكاء لا يمنع الإرسال** — تحوّل صامت إلى نموذج ٣ حقول دون فقد ما كُتب.

---

## المرحلة ٣ — التشغيل والتوسّع ⬜ لم تبدأ

- [ ] تقرير الأسبوع (مجمّع فقط بلا نصوص خام)
- [ ] المؤشرات الستة: وضوح التحول · التغطية · إغلاق الحلقة · زمن الاستجابة (وسيط) · عمر الشائعة · تركّز القلق
- [ ] وظائف الاحتفاظ المجدولة (٣٠ يومًا · ١٢ شهرًا · ٦ أشهر · ٢٤ شهرًا)
- [ ] رحلة التحول
- [ ] إعدادات الصوت: مفتاح واحد، مطفأ افتراضيًا، ومقطع ≤ ٣ ثوانٍ عند نجاح الإرسال فقط
- [ ] PWA: مسودات وقراءة دون اتصال

**معايير القبول**

- توليد تقرير أسبوعي مجمّع بلا نص خام، مع احترام عتبة الظهور ٧.
- وظيفة الحذف تعمل ويبقى العدّاد المجمّع فقط.
- إغلاق الحلقة يُقاس تلقائيًا.

---

## أسئلة مفتوحة تسبق المرحلة ١

هذه قرارات منتج لا يجوز افتراضها:

1. **اسم المنشأة والتجمّع** الذي سيظهر في الواجهة، وهل يلزم لوكاب مشترك مع شعار التجمّع.
2. **من يعتمد نصوص الإجابات رسميًا**، ومن البديل عند غيابه.
3. **قائمة الأقسام** للحقل الاختياري (مع استبعاد ما يقل عدد موظفيه عن ٧).
4. **مزوّد الاستضافة وقاعدة البيانات** المعتمد أمنيًا.
5. **بوابة نماذج الذكاء الاصطناعي** في المرحلة ٢، وأي مزوّد بعد موافقة الأمن السيبراني.

## أصول ناقصة

| الأصل | الأثر الحالي |
|---|---|
| `public/fonts/JannaLT-Regular.woff2` و`-Bold.woff2` | الخط يعمل عبر `local()` على الأجهزة التي عليه الخط، وإلا يسقط إلى خط النظام |
| النسخة البيضاء الرسمية للشعار | تُقلب النسخة الملوّنة عبر مرشّح CSS مؤقتًا |
| مقطع الصوت ≤ ٣ ثوانٍ | يُقتطع في المرحلة ٣ من `public/sound/sonic-logo-short.mp3` |
