# برومبت البناء التنفيذي — موقع «جسر التحول» بخدماته الفعلية

> هذا ليس برومبت تخطيط. هذه تعليمات بناء موقع يعمل فعليًا من أول تشغيل: كل خدمة موصولة بقاعدة بيانات حقيقية، وبمحتوى عربي جاهز، ومسار كامل من إرسال الموظف حتى نشر الإجابة.
> **الصق القسم ٠ في Claude Code، وضع باقي الملف باسم `SPEC.md` في جذر المشروع.**

---

## ٠ · الرسالة الأولى

```
اقرأ SPEC.md كاملًا، ثم ابنِ الموقع كاملًا وفق ما فيه — لا هيكلًا ولا نموذجًا أوليًا.

المخرج المطلوب عند انتهائك:
- المستودع على GitHub وفيه تاريخ commits نظيف، والفرع main محمي.
- الموقع منشور على Vercel برابط إنتاج يعمل، وكل PR يولّد معاينة تلقائيًا.
- قاعدة البيانات على Supabase، والترحيلات مطبّقة عليها، والبذر مزروع فيها.
- `pnpm i && pnpm dev` محليًا يعمل على المنفذ 3000 مقابل قاعدة Supabase نفسها.
- الخدمات الثماني في القسم ٣ تعمل كلها فعليًا من الواجهة إلى قاعدة البيانات ورجوعًا.
- المحتوى العربي في القسم ٦ مزروع في القاعدة، فلا تظهر أي شاشة فارغة عند أول فتح.
- `pnpm lint && pnpm typecheck && pnpm test && pnpm e2e` كلها خضراء.

اعمل بترتيب القسم ١٢، والتزم بالقسم ١٣. لا تسألني إلا إذا تعارض طلب مع مبدأ في القسم ١.
ابدأ الآن بالخطوة ١ ولا تنتظر موافقة بين الخطوات.
```

---

## ١ · مبادئ حاكمة (أي كود يخالفها يُرفض)

1. لا حقل ولا عمود لاسم أو رقم وظيفي أو بريد أو جوال أو تسجيل دخول للموظف — في أي مكان.
2. لا تسجيل لعناوين IP، ولا أدوات تحليلات طرف ثالث في واجهة الموظف.
3. تنقية النص على الجهاز ثم على الخادم؛ يُخزَّن المنقّى فقط ولا تُكتب النسخة الأصلية في أي سجل.
4. لا يُعرض تقسيم حسب القسم قبل وجود ٧ مشاركات داخله.
5. لا نشر تلقائي: كل ما يظهر للموظف كإجابة أو موقف من شائعة يمر باعتماد بشري من اللوحة.
6. لكل مشاركة حالة ظاهرة عبر رمز المتابعة. الصمت عيب وظيفي.
7. حين لا يوجد قرار رسمي، النص حرفيًا: «لم يصدر توضيح رسمي حتى الآن».

---

## ٢ · المكدس وبنية المشروع

```
Next.js (App Router) · TypeScript strict · Tailwind · Drizzle ORM + Postgres · Zod · Vitest · Playwright

app/
  layout.tsx                 dir="rtl" · خطوط Janna محليًا · لا كوكيز تتبع
  page.tsx                   S-01 الرئيسية
  ask/page.tsx               S-02 المحادثة (بارامتر gate + t)
  ask/review/page.tsx        S-03 المراجعة قبل الإرسال
  ask/done/page.tsx          S-04 تم الإرسال + الرمز
  track/page.tsx             S-05 متابعتي
  issues/page.tsx            S-06 القضايا الحيّة
  rumors/page.tsx            S-07 سمعت أن…
  knowledge/page.tsx         S-08 جسر المعرفة (3 تبويبات)
  answers/page.tsx           S-09 الأسئلة المتكررة الحيّة
  pulse/page.tsx             S-10 نبض التحول
  journey/page.tsx           S-11 رحلة التحول
  admin/page.tsx             S-12 لوحة سفير التغيير
  admin/login/page.tsx       دخول برمز واحد من البيئة
  api/**/route.ts            عقود القسم ٥
components/ui/               GateCard ChatBubble Chip StatusTag IssueCard BottomTabs Sheet
components/app/              الشاشات المركّبة
lib/redact.ts                التنقية
lib/classify.ts              التصنيف بالقواعد + واجهة مزوّد اختيارية
lib/cluster.ts               التجميع
lib/refcode.ts               توليد وتلبيد رمز المتابعة
lib/device.ts                معرّف الجهاز المحلي الدوّار
lib/rate.ts                  تحديد المعدّل
db/schema.ts db/seed.ts db/migrations/
tests/  e2e/
```

سكربتات `package.json`: `dev build start lint typecheck test e2e db:push db:seed db:studio`.

---

## ٢-ب · التسليم: GitHub + Vercel + Supabase

**نفّذ هذا القسم أولًا، قبل أي كود منتج.** كل خطوة لاحقة يجب أن تُدفع إلى المستودع وتُنشر تلقائيًا.

### أ · GitHub
```bash
git init && git branch -M main
gh repo create jisr-altahawwul --private --source=. --remote=origin
```
- `.gitignore` يشمل: `.env*` (عدا `.env.example`) · `node_modules` · `.next` · `playwright-report`.
- أول commit: `chore: bootstrap project` — ثم commit مستقل لكل خطوة من القسم ١٢.
- حماية `main`: لا دفع مباشر، والدمج عبر PR بعد نجاح الفحوصات.
- **لا تدفع أي سر إطلاقًا.** إن ظهر مفتاح في التاريخ، أوقف كل شيء وأبلغني فورًا.

### ب · Supabase (قاعدة البيانات)
- أنشئ مشروعًا واحدًا للإنتاج، واختر **أقرب منطقة معتمدة للمملكة**، ودوّن اسم المنطقة في `DECISIONS.md` لأن موقع تخزين البيانات بند امتثال لا تفصيل تقني.
- فعّل الإضافات: `pgcrypto` و`pg_cron` و`vector` (الأخيرة للتجميع الدلالي مستقبلًا).
- الاتصال: `DATABASE_URL` عبر **الـ pooler** (منفذ 6543) لدوال الخادم، و`DIRECT_URL` (منفذ 5432) للترحيلات فقط.
- **الأمان:** فعّل RLS على كل جدول واجعل السياسة الافتراضية رفضًا كاملًا لدور `anon`. التطبيق يصل عبر الخادم فقط. **لا تستخدم `supabase-js` في المتصفح، ولا تنشر أي مفتاح Supabase في كود العميل.**
- عطّل Supabase Auth ولا تنشئ أي مستخدم — المنصة بلا حسابات للموظفين بحكم المبدأ ١.
- الترحيلات: `drizzle-kit generate` ثم `drizzle-kit migrate` على `DIRECT_URL`، والملفات تُرفع مع الكود. **لا ترحيل تلقائي عند الطلب في زمن التشغيل.**
- البذر: `pnpm db:seed` يعمل على Supabase مباشرة، وقابل لإعادة التشغيل بلا تكرار (upsert بمفتاح ثابت).
- الاحتفاظ: وظيفة `pg_cron` يومية تنفّذ سياسات القسم ١٠، أو Vercel Cron إن كان أوضح.

### ج · Vercel (النشر)
```bash
vercel link
vercel env add DATABASE_URL production   # وكرّرها لكل متغير ولكل بيئة
vercel --prod
```
- اربط المشروع بمستودع GitHub ليصير النشر تلقائيًا: `main` ← إنتاج، وكل PR ← معاينة.
- **منطقة الدوال:** اختر الأقرب لقاعدة Supabase لتقليل زمن الاستجابة، ودوّن الاختيار.
- استخدم Node runtime لمسارات قاعدة البيانات، لا Edge.
- **عطّل Vercel Analytics و Speed Insights على واجهة الموظف** — المبدأ ٢ يمنع تحليلات الطرف الثالث هناك. يُسمح بها على مسارات `/admin` فقط إن لزم.
- لا تسجّل عناوين IP في أي middleware أو logger، وأزل أي حقل `ip` من مخرجات السجلات.

### د · المتغيرات (`.env.example`)
```
DATABASE_URL=            # Supabase pooler (6543)
DIRECT_URL=              # Supabase direct (5432) — للترحيلات
REF_CODE_PEPPER=         # openssl rand -hex 32
ADMIN_PASSCODE=          # رمز دخول لوحة السفير
CRON_SECRET=             # ترويسة تحمي مسار الاحتفاظ
```
نفس القائمة تُضاف في Vercel للبيئات الثلاث، وفي أسرار GitHub Actions لتشغيل الاختبارات.

### هـ · CI (`.github/workflows/ci.yml`)
على كل PR: `pnpm lint` · `pnpm typecheck` · `pnpm test` · `pnpm e2e` · **فحص حظر أعمدة الهوية** (القسم ١١-٣).
البناء يفشل إن سقط أي منها، ولا يُدمج PR أحمر.

---

## ٣ · الخدمات الثماني — كل واحدة تعمل من طرف إلى طرف

### خدمة ١ · الإرسال عبر البوابات الأربع
المسار: الرئيسية ← اختيار بوابة ← `/ask?gate=question|concern|challenge|idea` ← المراجعة ← الإرسال ← الرمز.
- المحادثة في النسخة الأولى **بلا نموذج ذكاء**: سؤال افتتاحي ثابت حسب البوابة، ثم سؤالان ثابتان للمتابعة مبنيان على قواعد (القسم ٧)، ورقائق اختيار مقترحة، وحقل كتابة حر دائم، وزر «إنهاء الآن» في كل خطوة.
- المسودة تُحفظ في `localStorage` تحت `bridge:draft` وتُستعاد عند العودة.
- عند الإرسال: تنقية ← تصنيف ← تجميع ← توليد رمز ← تخزين تلبيده ← عرض الرمز **مرة واحدة**.

### خدمة ٢ · المتابعة بالرمز
`/track` ← إدخال الرمز ← عرض الخط الزمني للحالات بتواريخها + عدد المشاركات المشابهة + زر حذف النص الخام (يبقي العدّاد).

### خدمة ٣ · القضايا الحيّة والتصويت
`/issues` ← أعلى ٦ قضايا بالوزن الأسبوعي ← زر «هذا يشغلني أيضًا» (صوت واحد لكل جهاز لكل قضية، وحد ٦ أصوات/جهاز/يوم) ← القضية المُجابة تحمل رابطًا للإجابة.

### خدمة ٤ · سمعت أن…
`/rumors` ← إدخال الادّعاء ← تطبيع النص ← دمج مع ادّعاء مشابه أو إنشاء جديد ← يظهر على اللوح **فقط بعد** اعتماد موقف من اللوحة. الحالات أربع لا خامس لها: `waiting | false | true | partly`. أي ادّعاء يتضمن اسم شخص يُحجب آليًا ويُحوَّل لمراجعة يدوية. عند بلوغ ١٠ تكرارات يُعلَّم `needs_escalation` في اللوحة.

### خدمة ٥ · جسر المعرفة
`/knowledge` بثلاثة تبويبات: `known` / `unclear` / `changed`. كل بطاقة: عنوان + نص + **مصدر** + تاريخ. بطاقة بلا مصدر لا تُنشر (تحقق في المخطط: `source NOT NULL`). تبويب «ما الذي تغيّر» يُبنى تلقائيًا من البطاقات التي لها `superseded_by`.

### خدمة ٦ · الأسئلة المتكررة الحيّة
`/answers` ← فلاتر بالموضوع ← كل عنصر: السؤال + الإجابة + المصدر + آخر تحديث + عدد المهتمين + الحالة. الترتيب: المُجاب حديثًا أولًا ثم الأعلى اهتمامًا. السؤال بلا إجابة **يبقى ظاهرًا بحالته**. البحث بلا نتيجة يفتح المحادثة بالنص المكتوب مسبقًا.

### خدمة ٧ · نبض التحول
`/pulse` ← سؤال أسبوعي واحد (١–٥) + حقل نصي اختياري ← رد واحد لكل جهاز لكل أسبوع ← بعد الإرسال يرى الموظف **النتيجة المجمّعة نفسها** التي تراها الإدارة. لا يُعلن أسبوع تقل عيّنته عن ٢٠ ردًا؛ تُعرض بدلها: «العيّنة غير كافية لإعلان النتيجة».

### خدمة ٨ · لوحة سفير التغيير
`/admin` بعد رمز دخول من `ADMIN_PASSCODE` (جلسة موقّعة، بلا حساب مستخدم).
- **الوارد** مرتّب بالإلحاح المحسوب = `0.5×تكرار + 0.3×حداثة + 0.2×إشارات القلق`.
- إجراءات على المشاركة: قبول التصنيف المقترح أو تغييره · دمج في قضية أو إنشاء قضية · إحالة (تبدأ عدّاد SLA) · نشر إجابة معتمدة (حقل مصدر إلزامي) · إغلاق (خارج النطاق / تحويل للقناة الرسمية بسبب مكتوب).
- **الشائعات**: تعيين الموقف ونصه الرسمي ثم النشر.
- **تقرير الأسبوع**: زر يولّد ملخصًا مجمّعًا (بلا نص خام) قابلًا للنسخ.
- كل فعل إداري يُكتب في `audit_log`.

---

## ٤ · دورة حياة المشاركة (نفّذها كآلة حالات صريحة)

```
new → reviewed → needs_referral → referred → answered → published
جانبية: merged · out_of_scope · redirected · deleted_by_author
```
انتقالات مسموحة فقط عبر `lib/state.ts`، وكل انتقال يكتب سطرًا في `audit_log` وسطرًا في الخط الزمني الظاهر للموظف.
**SLA:** تصنيف ٢٤ ساعة · إحالة ٤٨ ساعة · إجابة ٧ أيام عمل · موقف من شائعة منتشرة ٤٨ ساعة. تجاوز السقف يظهر في اللوحة تحت «متأخرات»، وللموظف كنص: «تأخّر الرد عن الموعد — تمت متابعته مع فريق التحول».

---

## ٥ · عقود الـ API

```ts
POST /api/submissions
req  { gate:"question"|"concern"|"challenge"|"idea", body:string, answers?:Record<string,string>,
       dept?:string|null, deviceHash:string }
res  201 { refCode:"J7K4M2QP", topic:string, mergedIntoIssueId?:string, similarCount:number }
     429 { error:"rate_limited" }            // رفض صامت: لا تكشف القاعدة

GET  /api/track/:code            → { status, topic, similarCount, timeline:[{state,at}], slaDueAt }
DELETE /api/track/:code          → { deleted:true }        // يحذف body_clean ويبقي العدّاد
GET  /api/issues                 → [{ id,title,topic,weight,status,answerId,voted:boolean }]
POST /api/issues/:id/vote        → { weight:number }        // 409 إن صوّت الجهاز سابقًا
POST /api/rumors                 → { received:true }        // لا يعيد أي حالة نشر
GET  /api/rumors                 → المنشور المعتمد فقط
GET  /api/knowledge?kind=known|unclear|changed
GET  /api/faqs?topic=            → [{ question,answer,source,updatedAt,interestCount,status }]
POST /api/pulse                  → { week, average?:number, sample:number, published:boolean }
GET  /api/journey                → المراحل وحالاتها

// إدارية — تتطلب جلسة، وكل نداء يُسجَّل
GET  /api/admin/inbox?status=&topic=
POST /api/admin/submissions/:id/classify   { topic, type, urgency }
POST /api/admin/submissions/:id/merge      { issueId }
POST /api/admin/issues/:id/escalate        { toTeam, questionText }
POST /api/admin/answers                    { issueId, answer, source }   // source إلزامي
POST /api/admin/rumors/:id/verdict         { verdict, officialText }
GET  /api/admin/report/weekly              → مجمّع فقط
```

**الرمز:** ٨ خانات من `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (بلا O/0/I/1)، يُخزَّن `sha256(code + PEPPER)` فقط.
**معرّف الجهاز:** يُولَّد محليًا، يُخزَّن ملبّدًا، ويُجدَّد كل ٣٠ يومًا، ويُستخدم لمنع التكرار فقط.
**تحديد المعدّل:** إرسال ٥/ساعة · تصويت ٦/يوم · متابعة ٥ محاولات/ساعة · نبض ١/أسبوع — كلها بمعرّف الجهاز لا بالـ IP.

---

## ٦ · بيانات البذر (ازرعها حرفيًا — الموقع يجب أن يفتح ممتلئًا)

**المواضيع:** العقود · الرواتب والبدلات · الأمان الوظيفي · التقييم الوظيفي · النقل والتكليف · الهيكل والصلاحيات · الأنظمة والإجراءات · التدريب · التواصل والمعلومات · أخرى.

**جسر المعرفة — مؤكَّد:**
| العنوان | النص | المصدر |
|---|---|---|
| استمرارية صرف الرواتب | تستمر الرواتب بآليتها الحالية حتى إشعار رسمي لاحق. | تعميم فريق التحول |
| جهة الإشراف الإداري | الإشراف اليومي يبقى كما هو خلال المرحلة الانتقالية. | محضر اجتماع التحول |
| قنوات التواصل المعتمدة | الأسئلة تُستقبل عبر هذه المنصة وتُحال لفريق التحول. | سفير التغيير بالمنشأة |

**لم يتضح بعد:** موعد بدء الانتقال الفعلي · آلية احتساب البدلات بعد نقل العقود · مسار التظلم أثناء المرحلة الانتقالية. نص كل بطاقة: «لم يصدر توضيح رسمي حتى الآن».

**الأسئلة المتكررة:**
1. «ماذا سيحدث لعقود الموظفين؟» — `referred` · ١٦٢ مهتمًا · بلا إجابة.
2. «هل تتأثر مدة الخدمة السابقة؟» — `published` · ٩٤ مهتمًا · الإجابة: «تُحتسب مدة الخدمة السابقة كما هي، ولا يترتب على الانتقال إعادة احتسابها.» · المصدر: فريق التحول.
3. «متى تبدأ مرحلة الانتقال الفعلي؟» — `waiting` · ٧٧ مهتمًا.

**القضايا الحيّة (أوزان أولية):** العقود ٣٨ · الرواتب والبدلات ٣١ · الأمان الوظيفي ٢٧ · التقييم الوظيفي ١٩ · النقل والتكليف ١٤ · التدريب ٩.

**الشائعات:** «ستتوقف البدلات بعد التحول» — `false` · ٤٣ مرة · الرد: «لم يصدر أي قرار بإيقاف البدلات، وأي تعديل مستقبلي يصدر بتعميم رسمي.» — و«ستُعاد صياغة جميع العقود خلال شهر» — `waiting` · ٢١ مرة.

**رحلة التحول:** التوعية (مكتملة) · الاستعداد (**نحن هنا الآن**) · الانتقال · التطبيق · الاستقرار. لكل مرحلة: ماذا سيحدث · ماذا يحتاج الموظف · الأسئلة المفتوحة. **لا تزرع أي تاريخ غير رسمي.**

**النبض:** أربعة أسابيع سابقة بمتوسطات ٥٤ · ٦١ · ٦٨ · ٧٤ لعرض الاتجاه.

**تنبيه:** كل بيانات البذر مختلقة لغرض التشغيل. لا تزرع اسم موظف واحد، ولا تولّد بيانات تبدو حقيقية لأشخاص.

---

## ٧ · المنطق بلا ذكاء اصطناعي (النسخة الأولى)

**`lib/redact.ts`** — يُطبَّق على العميل ثم على الخادم:
- أرقام من ٤ خانات فأكثر · بريد إلكتروني · أرقام جوال بصيغها المحلية · أنماط «اسمي … / أنا …» · الرقم الوظيفي بصيغه الشائعة.
- الاستبدال بـ `[معلومة معرِّفة أُزيلت]`، وإرجاع `{clean, hits[]}` لعرض إشعار «أزلنا معلومة قد تدلّ عليك» في شاشة المراجعة.
- اختبار وحدة لكل نمط، واختبار يثبت أن النص الأصلي لا يظهر في أي سجل.

**`lib/classify.ts`** — تصنيف بالقواعد:
- خرائط كلمات مفتاحية لكل موضوع (عقد/تجديد/مدة ← العقود؛ بدل/راتب/علاوة ← الرواتب؛ نقل/تكليف ← النقل…).
- `type` من البوابة مباشرة، و`urgency` من إشارات القلق (خايف، قلق، ما أدري، متى) + التكرار.
- `confidence` = نسبة تطابق الكلمات؛ **أقل من ٠٫٦ ⟶ صندوق «يحتاج قراءة»** بلا تصنيف آلي.
- صدّر الواجهة `interface Classifier { classify(text, gate): Promise<Classification> }` وسجّل التنفيذ بالقواعد كمزوّد افتراضي، حتى يُستبدل لاحقًا بمزوّد نماذج **دون تغيير أي شاشة**.

**`lib/cluster.ts`** — التجميع: تطبيع النص (إزالة التشكيل، توحيد الألف والهمزة والتاء المربوطة) ثم تشابه Jaccard على الجذوع؛ ≥ 0.6 ⟶ دمج في القضية القائمة، وإلا قضية جديدة تُعرض في اللوحة للمراجعة.

**أسئلة المتابعة الثابتة:** بعد الرد الأول اسأل «ما الذي يجعل هذا الموضوع غير واضح لك؟» مع رقائق (لا يوجد توضيح رسمي / سمعت معلومة غير مؤكدة / التعليمات متضاربة / أخرى)، ثم «ما المعلومة التي لو وصلتك ستطمئنك؟». حد أقصى سؤالان، ولا تسأل إطلاقًا عن القسم أو المسمى أو مدة الخدمة.

---

## ٨ · الهوية (طبّقها كـ tokens لا كقيم متناثرة)

```css
--hh-2985:#2FA9E0; --hh-299:#1691D0; --hh-2736:#15508A;
--hh-072:#283A83; --hh-3145:#057590; --hh-7479:#3AC0C3; --hh-802:#48AD48; --hh-7543:#A09EA4;
```
- الأفعال والنصوص الزرقاء على `#15508A` فقط؛ `#2FA9E0` و`#1691D0` للأسطح والرسوم لا للنص على أبيض.
- الحالات: جديدة `#A09EA4` · أُحيلت `#1691D0` · بانتظار `#057590` · أُجيبت `#2F8A4A` · غير صحيح/تصعيد `#283A83` — ولكل حالة **لون + رمز + نص**.
- **Janna LT** بوزنَي Regular/Bold من `public/fonts` عبر `@font-face` محليًا. مقياس: ٢٢/١٦/١٥/١٣/١٢px.
- نقش «Flow of Care» فاتحًا على الأسطح الداكنة فقط بشفافية ٢٠–٥٠٪ وأسفل السطح، ولا يظهر خلف نص.
- الشعار: النسخة البيضاء على الداكن، حد أدنى ١٣٢px للكامل و٣٨px للأيقونة، ومساحة حماية = مربع واحد.
- أصغر مساحة لمس ٤٤×٤٤px · خصائص منطقية فقط للـ RTL · أرقام عربية-هندية في الواجهة، ولاتينية داخل `dir="ltr"` للرموز.
- **الصوت مطفأ عند أول فتح**؛ مفتاح واحد في الإعدادات، ونغمة ≤ ٣ ثوانٍ عند نجاح الإرسال فقط، ولا شيء إن كان الجهاز صامتًا.

---

## ٩ · النصوص المعتمدة (حرفيًا)

| الموضع | النص |
|---|---|
| الوعد | التحول يبدأ بالوضوح — اسأل، شارك مخاوفك، اطرح تحدياتك، وتابع الإجابات. |
| الشعار | اسأل • شارك • افهم • تابع |
| الخصوصية | مشاركتك مجهولة. لا نطلب اسمك ولا رقمك الوظيفي. لا تذكر أي بيانات تعريفية في نصّك. |
| زر الإرسال | إرسال المشاركة ← بعد النجاح: أُرسلت |
| الرمز | احفظ الرمز — لا يمكن استرجاعه إذا فُقد. |
| لا معلومة | لم يصدر توضيح رسمي حتى الآن. |
| تأخّر الرد | تأخّر الرد عن الموعد — تمت متابعته مع فريق التحول. |
| خارج النطاق | هذا الموضوع خارج نطاق قناة التحول. القناة المناسبة له: (…) |
| رمز خاطئ | لا توجد مشاركة بهذا الرمز. تحقّق من الحروف والأرقام. |
| دون اتصال | لا يوجد اتصال. حفظنا ما كتبت وسنرسله عند عودة الشبكة. |
| فراغ القضايا | لم يُفتح موضوع بعد. كن أول من يطرح ما يشغله. |
| عيّنة قليلة | العيّنة غير كافية لإعلان النتيجة هذا الأسبوع. |

لغة مباشرة موجّهة للموظف، بلا صيغ حكومية تقليدية («يتقدم التجمع بـ…»)، وبلا وعود أو «غالبًا/يُتوقّع» في نص رسمي، وبلا أسماء أشخاص في أي محتوى منشور.

---

## ١٠ · حالات إلزامية لكل شاشة

فراغ · تحميل (هيكل عظمي لا دوّارة) · خطأ مع إعادة محاولة · دون اتصال (المسودة محفوظة والإرسال مؤجل) · نجاح.
**الإتاحة:** تباين نص ≥ ٤٫٥:١ · تكبير حتى ٢٠٠٪ بلا كسر · تسميات لقارئ الشاشة على كل زر ورقاقة · ترتيب تنقّل منطقي · احترام `prefers-reduced-motion` · لا حالة تُنقل باللون وحده.

---

## ١١ · تعريف «منجز»

1. `pnpm test` يغطي: التنقية (كل نمط) · توليد الرمز وتلبيده · آلة الحالات (انتقال ممنوع يُرفض) · حساب الوزن والإلحاح · عتبة الـ ٧ · التصنيف بالقواعد.
2. `pnpm e2e` يمر بهذين المسارين:
   - **مسار الموظف:** فتح الرئيسية ← بوابة «عندي مخاوف» ← كتابة نص يحوي رقم جوال ← رؤية إشعار التنقية ← الإرسال ← نسخ الرمز ← `/track` ← ظهور الحالة `new`.
   - **مسار السفير:** دخول `/admin` ← تصنيف المشاركة ← دمجها في قضية ← إحالتها ← نشر إجابة بمصدر ← رجوع الموظف إلى `/track` ورؤية `answered` ← ظهور السؤال في `/answers`.
3. فحص آلي في CI يفشل البناء إذا ظهر في `db/schema.ts` أي من: `name, employee_id, email, phone, national_id, iqama`.
4. Lighthouse على الجوال: الأداء ≥ ٨٥، الإتاحة ≥ ٩٥.
5. `README.md` يشرح التشغيل والمتغيرات وروابط المستودع والإنتاج، و`.env.example` كما في القسم ٢-ب-د.
6. **التسليم مكتمل فعليًا:** المستودع على GitHub · رابط إنتاج Vercel يفتح الموقع ممتلئًا بالبذر · الترحيلات مطبّقة على Supabase · معاينة PR تعمل · CI أخضر على `main`.
7. اذكر في ردك الأخير: رابط المستودع، ورابط الإنتاج، واسم مشروع Supabase ومنطقته.

---

## ١٢ · ترتيب التنفيذ

0. **البنية والتسليم أولًا** (القسم ٢-ب): مستودع GitHub · مشروع Supabase بالترحيل الأول · ربط Vercel · CI · نشر صفحة أولى فارغة للتأكد أن خط التسليم يعمل من طرف إلى طرف.
1. التهيئة + الخطوط + tokens + مكوّنات `components/ui` بحالاتها + صفحة `/kitchen-sink`.
2. المخطط والترحيلات والبذر (القسم ٦) — تأكد أن كل شاشة ستجد بيانات.
3. `lib/redact.ts` و`lib/refcode.ts` و`lib/state.ts` مع اختباراتها **قبل** أي واجهة.
4. مسار الإرسال كاملًا: S-01 → S-02 → S-03 → S-04.
5. المتابعة والحذف: S-05.
6. الخدمات القرائية: S-06 و S-08 و S-09 و S-11.
7. الشائعات والنبض: S-07 و S-10.
8. لوحة السفير كاملة + تقرير الأسبوع + `audit_log`.
9. PWA: manifest + مسودات دون اتصال + تخزين المحتوى المنشور مؤقتًا.
10. الاختبارات والأداء والإتاحة، ثم `README`.
11. نشر الإنتاج النهائي، والتحقق من أن الموقع المنشور يطابق المحلي، ثم تسليم الروابط.

---

## ١٣ · قواعد العمل

1. لا تفترض قرار منتج غير مذكور هنا؛ إن اضطررت، اختر الخيار الأقل كشفًا للبيانات ودوّنه في `DECISIONS.md`.
2. لا مفاتيح ولا أسرار في الكود، ولا بيانات تبدو حقيقية لأشخاص في البذر.
3. أي ميزة تمسّ الخصوصية تحتاج اختبارًا يثبت أن البيانات المعرِّفة لا تصل إلى التخزين.
4. لا مكتبة UI جاهزة، ولا لون خارج لوحة القسم ٨، ولا خط غير Janna LT.
5. رسائل commit إنجليزية بصيغة conventional commits، وواحدة لكل خطوة من القسم ١٢.
6. عند انتهاء كل خطوة: شغّل الفحوصات واذكر في ثلاثة أسطر ما بُني وما تبقّى — ثم أكمل دون انتظار.
7. الواجهة والنصوص عربية، والكود والأسماء والملفات إنجليزية.
