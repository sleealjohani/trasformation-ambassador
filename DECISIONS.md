# قرارات التنفيذ

> قرارات لم يحسمها `SPEC.md` صراحة، اختير فيها الخيار الأقل كشفًا للبيانات. تُحدَّث مع كل خطوة.

| # | القرار | السبب |
|---|---|---|
| 1 | **المستودع:** العمل على المستودع القائم `sleealjohani/trasformation-ambassador` وفرع `claude/bridge-transformation-platform-f2g2re`، لا إنشاء `jisr-altahawwul` جديد. | المستودع موجود مسبقًا ومربوط بالجلسة؛ إنشاء مستودع ثانٍ يشتّت التاريخ. حماية `main` تُضبط من إعدادات GitHub (تتطلب صلاحية مالك). |
| 2 | **منطقة Supabase:** `ap-northeast-2` (سيول) — المشروع الذي فتحه صاحب الحساب. | لا تتوفر منطقة داخل المملكة ولا في الخليج في Supabase. سيول أبعد من مومباي (`ap-south-1`) وأبعد أثرًا على زمن الاستجابة، ونقل المشروع لاحقًا ممكن بإعادة تشغيل الترحيل والبذر. **موقع تخزين البيانات بند امتثال يُراجَع مع الأمن السيبراني قبل الإطلاق الفعلي.** |
| 3 | **حساب الجهاز:** المعرّف يُولَّد عشوائيًا في المتصفح ويُلبَّد بـ SHA-256 قبل الإرسال، ثم يُلبَّد ثانية على الخادم مع `REF_CODE_PEPPER` قبل التخزين. | لا يصل الخام إلى الخادم ولا يصل تلبيد الخادم إلى المتصفح — لا طرف يملك الاثنين. |
| 4 | **الدمج في قضية** لا يُنهي حياة المشاركة: تبقى في المسار الرئيسي وترث حالة القضية (إحالة ← إجابة)، ويُسجَّل الدمج في الخط الزمني. الحالة الجانبية `merged` محجوزة لإغلاق التكرار الصريح من اللوحة. | مبدأ «لا صمت»: الموظف الذي دُمجت مشاركته يجب أن يرى الإجابة عبر رمزه. |
| 5 | **الحالات الست الظاهرة** للموظف: جديدة · تمت المراجعة · بانتظار توضيح · أُحيلت · تمت الإجابة · تصعيد. تجاوز SLA بعد الإحالة يظهر «تصعيد» مع النص المعتمد. | القائمة لم تُذكر صراحة؛ اشتُقّت من دورة الحياة وألوان القسم ٨. |
| 6 | **نبض التحول:** تُخزَّن المجاميع الأسبوعية في `pulse_weeks` وتُحدَّث مع كل رد؛ بذر الأسابيع الأربعة السابقة مجاميع فقط بلا ردود فردية مختلقة. | لا نولّد ردودًا فردية تبدو حقيقية. |
| 7 | **معرّفات البذر ثابتة** (UUID محجوزة) حتى تكون إعادة البذر upsert بلا تكرار. | مطلب SPEC §٢-ب. |
| 8 | **الصوت:** المقطع ≤ ٣ ثوانٍ يُقتطع من مطلع الشعار الصوتي القصير ويُشغَّل عند نجاح الإرسال فقط إن فعّله الموظف؛ الحالة في `localStorage` تحت `bridge:sound`. | SPEC §٨. |
| 9 | **بدون اتصال:** المسودة في `bridge:draft` والإرسال المؤجل في `bridge:outbox` يُعاد إرساله عند عودة الشبكة من الجهاز نفسه. | لا يُخزَّن شيء على الخادم قبل التنقية. |
| 10 | **الاحتفاظ:** أجوبة المتابعة ٣٠ يومًا · نص المشاركة ١٢ شهرًا · نص النبض ٦ أشهر · سجل التدقيق ٢٤ شهرًا، عبر Vercel Cron يوميًا على `/api/cron/retention` بترويسة `CRON_SECRET`. | Vercel Cron أوضح من `pg_cron` لأنه يعيش مع الكود. |

---

## حالة البنية التحتية

| البند | الحالة |
|---|---|
| **مشروع Supabase** | `Salwa's Project` · المرجع `woureabyrnjlgfxcbaea` · المنطقة `ap-northeast-2` |
| **المخطط** | مطبَّق: ١٥ جدولًا، والترحيل مسجَّل في `drizzle.__drizzle_migrations` فلا يُعاد تطبيقه |
| **البذر** | مزروع: ١٠ مواضيع · ٨ قضايا · إجابة معتمدة · ٣ أسئلة · ٧ بطاقات معرفة · شائعتان · ٥ مراحل · ٤ أسابيع نبض |
| **RLS** | مفعَّل على الجداول الخمسة عشر كلها بلا سياسة واحدة = رفض كامل لدورَي `anon` و`authenticated`، مع سحب كل الصلاحيات عنهما. التطبيق يصل بدور مالك الجداول من الخادم فقط. فاحص Supabase الأمني لا يُبلّغ عن شيء أعلى من INFO. |
| **الخطوط** | `JannaLT-Regular.woff2` و`JannaLT-Bold.woff2` مضمّنان في `public/fonts` بلا `local()` ولا شبكة خارجية |

| # | القرار | السبب |
|---|---|---|
| 11 | **لم أُنشئ دورًا جديدًا في قاعدة البيانات** ولم أولّد كلمة مرور. | منافذ Postgres الخام (5432 و6543) غير قابلة للوصول من بيئة التنفيذ هذه، فلا يمكنني التحقق من أي سلسلة اتصال أصنعها. سلسلتا الاتصال تُنسخان من لوحة Supabase مباشرة. |
| 12 | **الترحيل طُبِّق عبر واجهة Supabase لا عبر `drizzle-kit migrate`**، ثم سُجِّل في جدول Drizzle بالبصمة نفسها. | المنفذ المباشر غير متاح من هنا. النتيجة متطابقة: `pnpm db:migrate` لاحقًا يراه مطبَّقًا ولا يعيده. |

---

## عوائق التسليم الخارجي (ليست قرارات — تحتاج تدخّل صاحب الحساب)

| العائق | التفصيل | ما يلزم |
|---|---|---|
| **سلسلتا الاتصال** | كلمة مرور قاعدة البيانات لا تُسترجع عبر واجهة Supabase، ومنافذ Postgres الخام محجوبة من بيئة التنفيذ. | انسخ `DATABASE_URL` (pooler · 6543) و`DIRECT_URL` (direct · 5432) من لوحة Supabase إلى `.env.local` وإلى متغيرات Vercel. |
| **Vercel** | `list_teams` يعيد قائمة فارغة، و`list_projects` يفشل. لا فريق يمكن إنشاء المشروع تحته عبر الموصّل. | ربط حساب Vercel بالموصّل، أو إنشاء المشروع يدويًا وربطه بالمستودع. |
| **حماية `main`** | لا توجد أداة لضبط قواعد الحماية في هذه الجلسة. | تُضبط من إعدادات المستودع: منع الدفع المباشر، والدمج عبر PR بعد نجاح CI. |

المنطقة المقترحة عند فتح المشروع: **ap-south-1 (مومباي)** — أقرب منطقة تتيحها Supabase للمملكة،
ولا تتوفر منطقة داخل المملكة ولا في الخليج. موقع تخزين البيانات بند امتثال يُراجَع مع الأمن السيبراني
قبل الإنتاج الفعلي.
