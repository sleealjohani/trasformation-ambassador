# جسر التحول — صوت الموظف

> يقرأ Claude Code هذا الملف في بداية كل جلسة. `PLAN.md` يحمل المراحل ومعايير القبول.

---

## ١ · المشروع في فقرة

منصة ويب للجوال يشغّلها **سفير التغيير** في منشأة صحية أثناء الانتقال إلى **شركة الصحة القابضة**.
يفتحها الموظف من جواله بلا حساب، فيكتب سؤاله أو مخاوفه أو تحديًا يواجهه أو اقتراحًا أو شائعة سمعها.
تصل المشاركة **مجهولة المصدر** إلى لوحة سفير التغيير مُصنَّفة ومجمّعة مع ما يشبهها، فيحيلها إلى فريق
التحول، ثم تُنشر الإجابة الرسمية ويتابعها الموظف برمز.

**المشكلة:** الفراغ المعلوماتي أثناء التحول يُملأ بالشائعات، وفريق التحول لا يعرف أين الفجوة الحقيقية.

**مقياس النجاح الأول:** ≥ ٨٠٪ من الأسئلة المُحالة تُجاب خلال ٧ أيام عمل، وارتفاع مؤشر وضوح التحول
١٥ نقطة خلال ٩٠ يومًا.

---

## ٢ · المبادئ غير القابلة للتفاوض

هذه ليست تفضيلات. أي كود يخالفها يُرفض.

1. **مجهول بالتصميم لا بالوعد.** لا حقل اسم، ولا رقم وظيفي، ولا بريد، ولا جوال، ولا تسجيل دخول
   للموظف. **لا أعمدة لها في قاعدة البيانات أصلًا.**
2. **لا تسجيل لعناوين IP** في أي طبقة، ولا تحليلات طرف ثالث في واجهة الموظف.
3. **تنقية مزدوجة للنص:** على الجهاز أولًا ثم على الخادم. يُخزَّن النص المنقّى فقط، ولا تُحفظ النسخة
   الأصلية في أي مكان ولا في السجلات.
4. **عتبة الظهور ٧:** لا يُعرض أي تقسيم (قسم/فئة) في أي تقرير قبل وجود ٧ مشاركات داخله.
5. **الذكاء الاصطناعي يسأل ويصنّف ولا يُفتي.** لا يجيب عن سياسة، ولا يخمّن قرارًا، ولا يصل أي مخرج
   آلي إلى موظف أو إلى فريق التحول دون مراجعة بشرية.
6. **لا صمت.** كل مشاركة لها حالة ظاهرة للموظف عبر رمز المتابعة.
7. **حين لا يوجد قرار رسمي**، النص المعتمد حرفيًا: «لم يصدر توضيح رسمي حتى الآن».
8. **تسعون ثانية:** من فتح الرابط إلى تأكيد الإرسال، بأقل عدد لمسات ممكن.
9. **التطبيق صامت افتراضيًا.** لا صوت في أي مسار إرسال؛ التفاصيل في `public/sound/README.md`.

---

## ٣ · المكدس التقني

| الطبقة | الاختيار | ملاحظة |
|---|---|---|
| الإطار | Next.js 16 (App Router) + TypeScript صارم | `strict: true` · لا `any` |
| الواجهة | Tailwind CSS 4 + مكوّنات خاصة | لا مكتبة UI جاهزة |
| الحالة | React Server Components + Server Actions | العميل خفيف قدر الإمكان |
| قاعدة البيانات | PostgreSQL + `pgvector` | المزوّد لم يُعتمد بعد — سؤال مفتوح |
| ORM | Drizzle ORM + drizzle-kit | الترحيلات في `db/migrations` |
| التحقق | Zod على كل مدخل | لا ثقة بأي بيانات واردة |
| الذكاء | بوابة نماذج خلف خدمة داخلية | لا نداء مباشر من المتصفح |
| PWA | manifest + service worker (المرحلة ٣) | المسودات والقراءة دون اتصال |
| الاختبار | Vitest للوحدات + Playwright للمسارات | |
| النشر | لم يُعتمد بعد — سؤال مفتوح | مع مسار خروج لاستضافة داخلية |

**ميزانية الأداء:** أول رسم للمحتوى < ١٫٨ ثانية على 4G، والحزمة الأولى < ١٨٠KB مضغوطة
(يفحصها `pnpm budget` بعد `pnpm build`).

---

## ٤ · أوامر التشغيل

```bash
pnpm install          # التهيئة
pnpm dev              # خادم التطوير
pnpm build            # بناء الإنتاج
pnpm start            # تشغيل البناء

pnpm lint             # ESLint
pnpm typecheck        # tsc --noEmit
pnpm test             # Vitest
pnpm verify           # lint + typecheck + test معًا — يجب أن يمر قبل كل commit

pnpm e2e              # Playwright (يبني ويشغّل الخادم تلقائيًا)
pnpm budget           # ميزانية الحزمة الأولى (بعد pnpm build)

pnpm db:generate      # توليد ترحيل من المخطط
pnpm db:migrate       # تطبيق الترحيلات
```

بيئات تُثبّت Chromium خارج مسار Playwright الافتراضي: اضبط `PLAYWRIGHT_CHROMIUM_EXECUTABLE`
قبل `pnpm e2e`.

---

## ٥ · بنية المستودع

```
src/app/           صفحات App Router · globals.css يحمل design tokens
src/components/ui/ مكوّنات نظام التصميم السبعة + Icon
src/lib/           tokens.ts (مصدر واحد لقيم الهوية) · numerals.ts · cn.ts
src/content/       strings.ts — النصوص المعتمدة، تُستخدم حرفيًا
db/                schema.ts · client.ts · migrations/
tests/             اختبارات الوحدات (Vitest)
e2e/               اختبارات المسارات (Playwright)
public/brand/      الشعار والنقش الرسميان + قواعد استخدامهما
public/fonts/      Janna LT — الملفات المرخّصة تُوضع هنا ولا تُرفع للمستودع
public/sound/      الهوية الصوتية + قواعد الصوت
scripts/           فحوصات مساعدة (ميزانية الأداء)
```

---

## ٦ · الهوية البصرية باختصار

- **الألوان:** لوحة الصحة القابضة كاملة في `src/lib/tokens.ts` و`globals.css`. **لا لون خارجها** —
  يمنع ذلك اختبار `tests/palette.test.ts`.
- `#2FA9E0` و`#1691D0` **لا يُستخدمان لنص على أبيض**. النصوص والأفعال على `#15508A`.
- اللوحة بلا لون تحذيري أحمر: **كل حالة تُنقل بلون + رمز + نص، لا بلون وحده.**
- **الخط:** Janna LT بوزنين فقط (Regular · Bold). لا وزن وسيط ولا سماكة مخلّقة.
- **الأرقام** في الواجهة عربية-هندية عبر `toArabicDigits`؛ المعرّفات والرموز لاتينية داخل `dir="ltr"`.
- **RTL:** خصائص منطقية فقط (`margin-inline` · `inset-inline-start`)، وتُعكس الأيقونات الاتجاهية دون
  غيرها، والنص المختلط داخل `<bdi>`.
- **الشعار والنقش:** القواعد في `public/brand/README.md` — لا يُعدَّل الشعار ولا يُستحدث نقش بديل.

معرض المكوّنات بحالاتها كلها على `/kitchen-sink`.

---

## ٧ · قواعد العمل في كل جلسة

1. اقرأ `CLAUDE.md` و`PLAN.md` قبل أي عمل، واذكر في أول سطر: **المرحلة الحالية والمهمة التالية**.
2. اعمل بخطوات صغيرة قابلة للمراجعة: مهمة واحدة ← فحص ← ملخص ٥ أسطر ← انتظار الموافقة.
3. **اسأل ولا تفترض.** إن نقص قرار منتج (نص، حد، سلوك حالة)، اطرح سؤالًا واحدًا محددًا ثم توقف.
4. لا تعدّل ملفات خارج نطاق المهمة، ولا تعِد تنسيق كود لم يُطلب تعديله.
5. لا تكتب مفاتيح ولا بيانات حقيقية ولا أسماء موظفين في أي ملف أو seed. بيانات التجربة مختلقة بالكامل.
6. كل ميزة تمسّ الخصوصية تحتاج اختبارًا يثبت أن البيانات المعرِّفة لا تصل إلى التخزين.
7. رسائل commit بالإنجليزية وبصيغة conventional commits.
8. حدّث `PLAN.md` بعلامة إنجاز بعد كل مهمة مقبولة.
9. عند التعارض بين طلب وبين مبدأ من القسم ٢، **نبّه قبل التنفيذ**.
10. اللغة: واجهات ونصوص المستخدم بالعربية، والكود والأسماء والالتزامات بالإنجليزية.
11. بعد كل تغيير: `pnpm verify` — ولا تُنهِ مهمة والفحص أحمر.
